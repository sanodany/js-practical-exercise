
function count(string) {
    const obj1 = {}
  for (i=0;i<string.length;i++){
      let value =  string[i]
      if (value in obj1){
          obj1[value] += 1;
      } else if(!(value in obj1)){
          obj1[value] = 1
      }
  }
  return obj1;
}
console.log(count("sano dany"))
